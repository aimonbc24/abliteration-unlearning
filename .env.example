# Copy to .env (which is gitignored) and fill in.
HF_TOKEN=            # Hugging Face token with access to gated Meta-Llama-3-8B weights
OPENAI_API_KEY=      # used by the LLM judge and perturbation generation
WANDB_API_KEY=       # optional, experiment logging
